# j = 0; 
# i = 10; 
# do  
#  { 
#     j = j + 1; 
#  }  
# while ( j != i );

addi $s0, $zero, 0     # j = 0
addi $s1, $zero, 10    # i = 10

loop:
    addi $s0, $s0, 1       # j = j + 1
    bne  $s0, $s1, loop    # while (j != i) -> volta pro loop